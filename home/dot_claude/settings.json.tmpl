{{- $msysRoot := trimSuffix "/etc/config.site" (env "CONFIG_SITE") -}}
{
  "env": {
{{- if and (eq .chezmoi.os "windows") (ne $msysRoot "") }}
    "CLAUDE_CODE_GIT_BASH_PATH": "{{ $msysRoot }}/usr/bin/bash.exe",
    "BASH_ENV": "~/.claude/bash_env"
{{- end }}
  },
  "theme": "auto"
}
