# Starship config — minimal cross-platform
scan_timeout = 50
add_newline = false
format = "$directory$git_branch$git_status$character"

[directory]
truncation_length = 5
truncate_to_repo = true

[git_branch]
symbol = " "

[character]
success_symbol = "[❯](green)"
error_symbol = "[❯](red)"
