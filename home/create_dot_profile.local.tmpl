# ~/.profile.local — machine-specific profile customizations
# This file is sourced by ~/.profile (chezmoi-managed)
# Add your own PATH additions, environment variables, etc. here

# GitHub 加速镜像（国内直连慢）。删除此行即直连；改成其他前缀即换镜像。
export GH_PROXY=https://ghfast.top/
