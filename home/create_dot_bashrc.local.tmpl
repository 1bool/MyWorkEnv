# ~/.bashrc.local — machine-specific bash customizations
# This file is sourced by ~/.bashrc (chezmoi-managed)
# Add your own aliases, environment variables, etc. here

# GitHub 加速镜像（国内直连慢）。删除此行即直连；改成其他前缀即换镜像。
export GH_PROXY=https://ghfast.top/
